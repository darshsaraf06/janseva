# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/janseva_db
spring.datasource.username=root
spring.datasource.password=Ishitamom@2
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Google Cloud Configuration (Important!)
# You need to set up Google Cloud Authentication. The easiest way for local dev
# is to install the gcloud CLI and run `gcloud auth application-default login`.
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json